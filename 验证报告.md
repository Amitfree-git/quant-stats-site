# 离线网站验证报告

验证日期：2026-08-30

## 自动化检查结果

- JavaScript 语法检查：通过；
- 内容结构：1 个课前预备章节 + 30 个核心课程，共 31 个学习单元；
- 课程模块：8 个；
- 交互实验：21 个，其中新增 4 个课前实验；
- P00 位于课程序列首位：通过；
- 课程、项目和资源 ID 唯一性：通过；
- Chromium 单文件 HTML 加载与路由切换：通过；
- 首页 8 个模块、课程目录 31 个单元、进度页 31 个单元：通过；
- P00 正文、84 项页内目录与 MathJax 公式渲染：通过；
- 四个课前实验逐项加载、指标初始化、Canvas 图表和滑块联动：通过；
- 全文搜索能够检索“初中数学”预备章节：通过；
- 浏览器控制台错误：0；
- 单文件 HTML 资源内联：通过；
- 外部运行时依赖：0。

## 新增内容检查

- 正负数、百分数、百分点和基点：已覆盖；
- 代数式、运算顺序、公式变形与单位检查：已覆盖；
- 函数、坐标、斜率和截距：已覆盖；
- 平方、平方根、离差、方差与标准差：已覆盖；
- 指数、复利、对数和平方根年化：已覆盖；
- 下标、求和、乘积、平均数和加权平均：已覆盖；
- 集合、事件、频率、概率和条件概率：已覆盖；
- 数学符号到 NumPy 数组运算：已覆盖；
- 15 题课前自测、七天学习计划和验收清单：已覆盖。

## 验证边界

当前自动化运行环境的 Chromium 策略阻止直接导航到 `file://` 和 `127.0.0.1`，因此本次浏览器回归测试使用了完全内联的单文件 HTML。多文件版的 JavaScript、相对资源路径和文件清单已经独立校验，但本次未在该受限浏览器中重新执行直接 `file://` 导航。

## 设计边界

1. 预备实验用于建立数学直觉，不替代系统的代数或概率教材。
2. 波动率平方根年化只在相应依赖结构假设下成立；正式研究应检验自相关和异方差。
3. 浏览器端实验为教学实现，生产统计结果仍应由经过验证的 Python/R 统计库交叉复核。
4. LocalStorage 不是长期数据库，建议定期导出学习记录 JSON。
